<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SWIM.ViewModels"
             x:Class="SWIM.Views.PaymentExtension">

    <ContentPage.BindingContext>
        <local:PaymentExtensionViewModel/>
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        <Grid>
            <Label Text="Bill amount: "/>
            <Label Grid.Column="1" 
                   Text="$$$"/>
            
            <Label Grid.Row="1" Text="Orginal Due Date: "/>
            <Label Grid.Row="1" 
                   Grid.Column="1"
                   Text="Date"/>

            <Label Grid.Row="2"
                   Text="Select a preferred date:"/>
            <DatePicker Grid.Row="2" 
                        Grid.Column="1" 
                        MinimumDate="{Binding CurrentDate}"
                        MaximumDate="{Binding MaximumDate}"/>

            <Button Grid.Row="3"
                    Grid.ColumnSpan="2"
                    Text="Request Extension"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>